# Overview
Daily notes to keep myself on track with this project. It's Saturday, so I should be able to get a ton done.

##TODOs:
- [x] Set up migrations to use uuid instead of text for PKs and FKs
  - Will only apply to the users and recipes table, haven't set anything else up yet.
- Initial layer completion:
  - [X] Finish the recipes service layer
  - [ ] Finish the recipes controller
    - This should be easy to do, but there has to be a better way to register routes that what I've seen. Most people dump them into main and that doesn't make any sense to me.
- [ ] Unit tests, endpoint testing.

## Changes 
There have been major changes to the codebase now that I need to set up the router. I want to make this as simple as possible, and through all of my googling, centralizing each layer seems to be the best way to go. 

We can initialize each service based on its required dependencies. This will limit criss crossed service -> repository calls. If I find that I need to allow that for whatever reason (unlikely that will happen, we can use cross service calls instead), then I can expose every repository to every service. 

I don't think I'll need to do that, but it is possible if required. It's 2:30 as I'm writing this and it might be incoherent. Praying that when I read this again it will make sense. 

I hope it works! I'll either be thanking or cursing myself shortly. I guess we'll see. 

